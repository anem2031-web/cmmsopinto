import { router } from "../_shared/procedures";
// Reserved as a future barrel router for purchase domain.
// Currently purchase sub-routers (purchaseOrders, approvals, vendors)
// are registered individually in index.ts.
export const purchaseRouter = router({});
