// Shared middleware — expand as needed.
// roleMiddleware lives in procedures.ts because it returns a typed procedure.
// Place here: logging middleware, rate limiting middleware, context enrichment.

export {};
