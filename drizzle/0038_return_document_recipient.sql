-- ============================================================
-- 0038_return_document_recipient.sql
-- إضافة "اسم المستلم" (من استلم الصنف المرتجَع، مثل المندوب/المورد)
-- ليظهر كخانة توقيع ثانية بوثيقة المرتجع
-- ============================================================

ALTER TABLE `return_documents`
  ADD COLUMN `recipientName` VARCHAR(200) NULL COMMENT 'من استلم الصنف المرتجَع (مندوب/مورد)' AFTER `returnedByName`;
